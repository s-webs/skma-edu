lightGallery(document.getElementById('slideshow'), {
    plugins: [lgFullscreen],
    speed: 500,
    mode: 'lg-slide',
    animateThumb: true, 
    showThumbByDefault: true, 
    download: false
});